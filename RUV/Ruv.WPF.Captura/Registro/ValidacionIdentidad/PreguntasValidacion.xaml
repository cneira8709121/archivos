<Page x:Class="Ruv.WPF.Captura.Registro.ValidacionIdentidad.PreguntasValidacion"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Loaded="Window_Loaded"
        Title="Preguntas de Validacion" Height="550" Width="800">
    <Grid>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="30"  />
                <RowDefinition />
                <RowDefinition Height="50" />
            </Grid.RowDefinitions>
            <Label Content="Preguntas de Validación" Grid.Row="0" HorizontalAlignment="Center" Grid.RowSpan="2" ></Label>
            <ListBox x:Name="lbPreguntas" Margin="10,10,10,10" Width="700" Grid.Row="1">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <GroupBox Header="{Binding Pregunta}" Margin="10,10" Width="660">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition></RowDefinition>
                                    <RowDefinition></RowDefinition>
                                </Grid.RowDefinitions>
                                <ListBox ItemsSource="{Binding OpcionesPreguntas}" Grid.Row="1">
                                    <ListBox.ItemTemplate>
                                        <DataTemplate>
                                            <RadioButton IsChecked="{Binding Respuesta}" Content="{Binding PosibleOpcion}" GroupName="{Binding Pregunta}" ></RadioButton>
                                        </DataTemplate>
                                    </ListBox.ItemTemplate>
                                </ListBox>
                            </Grid>
                        </GroupBox>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
            <Button Content="Validar" Grid.Row="2" Height="25" Width="100" x:Name="btnValidar" Click="btnValidar_Click" ></Button>
        </Grid>
    </Grid>
</Page>
