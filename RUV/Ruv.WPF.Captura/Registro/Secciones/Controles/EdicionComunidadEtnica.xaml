<UserControl x:Class="Ruv.WPF.Captura.Registro.Secciones.Controles.EdicionComunidadEtnica"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="300"
             d:DesignWidth="300">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition />
    </Grid.RowDefinitions>
    <WrapPanel Orientation="Horizontal"
               Margin="5">
      <TextBlock>Filtro:</TextBlock>
      <TextBox Width="150"
               Margin="5,0"
               Text="{Binding TextoFiltro, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
      <Button Padding="5,0"
              Click="QuitarFiltro">Quitar filtro</Button>
    </WrapPanel>
    <ListBox x:Name="lbxComunidadesEtnicas"
             Grid.Row="1"
             Margin="5,0,5,5"
             ScrollViewer.VerticalScrollBarVisibility="Disabled"
             ScrollViewer.HorizontalScrollBarVisibility="Auto"
             SelectionChanged="ComunidadSeleccionada">
      <!--<ListBox.Resources>
        <Style TargetType="ListBoxItem">
          <Style.Resources>
            <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}"
                             Color="LightBlue" />
            <SolidColorBrush x:Key="{x:Static SystemColors.ControlBrushKey}"
                             Color="LightBlue" />
          </Style.Resources>
        </Style>
      </ListBox.Resources>-->
      <ListBox.ItemTemplate>
        <DataTemplate>
          <TextBlock Text="{Binding Nombre}"
                     Margin="0,0,7,0" />
        </DataTemplate>
      </ListBox.ItemTemplate>
      <ListBox.ItemsPanel>
        <ItemsPanelTemplate>
          <WrapPanel Orientation="Vertical" />
        </ItemsPanelTemplate>
      </ListBox.ItemsPanel>
      
    </ListBox>
  </Grid>
</UserControl>
