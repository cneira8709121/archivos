<UserControl xmlns:my="clr-namespace:Ruv.WPF.Captura.Impresion"
             x:Class="Ruv.WPF.Captura.Registro.Secciones.Controles.PersonaDesplazada"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:conv="clr-namespace:Ruv.WPF.Captura.Converters"
             mc:Ignorable="d"
             d:DesignHeight="40"
             d:DesignWidth="300">
  <Grid>
    <Grid.Resources>
      <conv:ConsultaDatoConverter x:Key="ConsultaDato" />
    </Grid.Resources>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="250"/>
      <ColumnDefinition Width="40" />
    </Grid.ColumnDefinitions>
    <Border BorderBrush="Black"
            BorderThickness="1"
            Grid.ColumnSpan="3" />
    <CheckBox x:Name="chkSeleccionado"
              VerticalAlignment="Center" 
              Margin="5">
      <TextBlock x:Name="txtNombre"
                 TextWrapping="Wrap"
                 VerticalAlignment="Center"
                 Grid.Column="1"
                 Text="{Binding PersonaAfectadaId, Converter={StaticResource ConsultaDato}, ConverterParameter=NOMBRECOMPLETOAFECTADO}" />
    </CheckBox>
    <my:DatoSiNoColumna x:Name="dsnSiNo"
                        Valor="{Binding SeDesplazo, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True, NotifyOnValidationError=True}"
                        DireccionTamaño="Both"
                        Grid.Column="2"
                        Margin="5"
                        Width="30"
                        Height="20" />
  </Grid>
</UserControl>
